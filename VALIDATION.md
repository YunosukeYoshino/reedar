# 検証記録

実施日: 2026-09-11 JST。macOS上のBun 1.3.12、Electron 42.11.1で実施。

## 自動検証

`bun run check` が通過。型チェック、37件のテスト、UIとElectronのビルドを確認した。

テスト対象: RSS/Atomの解析、HTML無害化、内部ネットワーク宛先の拒否、ローカルセッション認証とOrigin/Host検査、分割UTF-8入力、永続化と保存競合、会話継続、停止後の遅延出力、認証待ち、CLIのプロトコル処理、デスクトップのアセット参照、記事選択後のキーボード操作と入力欄の分離。

React DoctorはUI全体を確認した時点で84/100、エラーなし。条件分岐の複雑さ（App・記事一覧・本文）、Appの複数state、配列のfilter/mapに警告あり。具体的な不具合の指摘ではなく、現時点の小さなUI構成と読みやすさを優先して維持した。ボタンのtypeとリンクの意味付けは修正済み。その後の変更範囲のスキャンも通過している。スキャン範囲が違う点数を全体の改善値として比較しない。

## Computer Useによる実操作

### RSSと整理

Reedarのフォームから以下の実フィードを登録した。保存JSONを直接編集して登録したものではない。

| フィード | URL | 取得数 |
| --- | --- | --- |
| Zennのトレンド | https://zenn.dev/feed | 20 |
| Publickey | https://www.publickey1.jp/atom.xml | 15 |
| Hacker News: Front Page | https://hnrss.org/frontpage | 20 |

合計55記事。画面から「テクノロジー」フォルダを作成し、ZennとPublickeyを移動。各フィードの記事を開き、本文、既読件数、スター1件を確認した。記事選択後にJキーで次の記事へ移り、未読数が減ることもMacの実ウィンドウで確認した。

### Codex

アプリ同梱のCodex CLI 0.153.4、既存のChatGPT認証で実行。対象はPublickeyの「.NET 11リリース候補版が登場…」の記事。

1. 「この記事を日本語で要約して」をクリックし、日本語回答と原文リンク、「完了」を確認。
2. 「その要約のうち、本文で確認できることとタイトルだけから分かることを分けて教えて。」を送信。前の要約を踏まえた追加回答と「完了」を確認。
3. 長い回答を依頼。「回答中」のまま部分本文が増えていることを確認し、「回答を停止」をクリック。「中止」への変化と部分回答の保持を確認。

このフィードの本文は抜粋だった。Codexもその限界を明示し、本文で確認できる内容とタイトル由来の内容を区別した。全文取得済みとして扱っていない。

### Claude Code

CLIは検出できるが既存ログインがなく、接続画面で「ログインが必要」を表示。画面から要約を依頼し、「確認待ち」とログイン案内を確認した。認証や新規課金の操作は代行していない。

**実記事へのClaudeの要約・追加質問・ストリーミングは未検証。** ユーザーの指示で実接続確認を保留に変更した。今回のCodex確認・Antigravity調査を止める条件として扱わない。

### Macアプリと復元

ブラウザで作成したライブラリを、空だったMac版の保存先へコピーして起動。55記事、テクノロジーフォルダ、スター、既読、2種類のエージェントの会話を確認した。

初回Mac起動時にBunが `__dirname` をソースディレクトリに固定する不具合を検出。失敗するビルド回帰テストを追加し、アプリの場所を起点にアセットを解決するよう修正した。Macアプリを終了して再起動し、3ペインと保存済みデータ、Codexの完了した2回答と中止した部分回答を確認した。最終の通常終了・再起動後も、55記事、未読51件、スター1件、フォルダ内未読33件、会話2件の保持を確認した。

## 主要な制約

- 記事はフィード内の本文・抜粋を使用。Webページの全文再抽出は未実装。
- 既存CLIの契約枠を使う。CLIの仕様・設定との互換性は将来変化し得る。
- ローカルJSONによる単一ユーザー向けの保存。大規模購読・巨大履歴の性能試験は未実施。
- 通常の停止と終了は部分回答を保存するが、強制終了直前の未保存デルタを完全には復元できない。
- 署名・公証・配布用パッケージ、Inoreader同期、モバイルは対象外。

## 追加指定への対応

### GPT-5.3-Codex-Spark

Codex CLIの `model/list` で `gpt-5.3-codex-spark` が利用可能であり、mediumの推論設定を受け付けることを確認。`thread/start` と `turn/start` の両方にモデルを指定し、開始応答のモデル一致を検証する。モデル差し替えを拒否する自動テストも通過。

MacアプリからPublickeyの記事に「この記事の提供された本文を、日本語で3点に要約してください。」を送信し、3点の日本語要約と「Codex · Spark 完了」を確認。続けて「3点目について、提供された本文だけで確認できる範囲をもう少し詳しく教えてください。」を送信し、イベント案内の詳細と本文にない情報の区別、原文リンク、完了を確認した。既存のモデル不明な回答は従来のCodex表示を保持。

### Antigravity

接続設定にAntigravityを追加。インストール済み `agy` の検出と「連携準備中」の状態を実装。記事送信経路は明示的なエラーで止まり、Codexに誤って流れないことを自動テストで確認した。

実機では `agy models` が既存ログインでGemini等のモデル一覧を返した。記事を渡さない独立した一時ディレクトリでheadlessの初期化を調査。専用 `agent.md` の `tools: []` と `tools: [finish]`、後者では `--mode plan` も指定したが、`init.tools` にはファイル書き込み・コマンド・ブラウザ・MCP等が残り、`permission_mode` は `always-proceed` だった。このため本アプリからの記事送信は実装していない。ユーザー全体の権限・プラグイン設定は変更していない。

[公式headless仕様](https://antigravity.google/docs/cli/headless/)と[権限仕様](https://antigravity.google/docs/cli/permissions/)によると、headlessも既存設定を使い、既定でワークスペースの読み書きが許可される。[SDK](https://antigravity.google/docs/sdk/overview/)にはツールを制限する機能があるが、Gemini APIキーまたはVertex認証を要求するため、既存CLI契約のみで接続する今回の経路には採用しなかった。

残る課題は、既存Antigravityログインを利用しながら、読書セッション単位で外部操作を無効化できる接続方法の確立。Antigravityでの要約・追加質問は未実装・未検証。

追加変更後も `bun run check` は37件・109アサーションで全通過。React Doctorの変更範囲7ファイルは87/100、エラーなし。警告4件は既存のZod API使用とfilter/mapであり、今回追加した行への新規指摘はない。Macアプリを通常終了・再起動し、55記事とスター・フォルダの保持、接続画面のCodex「接続済み」とAntigravity「連携準備中」を確認。保存ファイルにもSparkの完了した要約・追加回答とモデル名が残っていた。
